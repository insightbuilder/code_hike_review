# Duplicate MDX file copied from the App's Root

This needs to automatically work... 

Codeblock example (from `./mdx-components.tsx`):

```js
const lorem = ipsum(dolor, sit)
// !callout[/amet/] This is a callout
const [amet, consectetur] = [0, 0]
lorem.adipiscing((sed, elit) => {
  if (sed) {
    amet += elit
  }
})
```

Layout examples:

- [/app/scrollycoding_test](/scrollycoding_folder)
